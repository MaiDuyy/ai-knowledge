# SecWiki-Bench hybrid evaluation (isolated .venv-bench)
# Install: python -m venv .venv-bench && .venv-bench\Scripts\pip install -r benchmark-eval/requirements.txt

ragas>=0.2.10,<0.3
datasets>=2.19.0
openai>=1.40.0
pandas>=2.2.0
python-dotenv>=1.0.0
langchain-openai>=0.1.0
langchain-google-genai>=2.0.0
langchain-community>=0.2.0
google-generativeai>=0.8.0
tiktoken>=0.7.0
numpy>=1.26.0

# Phase 2 (optional TruLens groundedness) — install separately if needed:
# trulens-eval>=0.33.0
